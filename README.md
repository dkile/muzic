# music-planner

Desktop-first Tauri app for local job search and application management.

## Development

```sh
pnpm dev
pnpm tauri dev
pnpm build
```

Project context lives in `docs/`. Start with `docs/CURRENT.md`.
